{
  "name": "worklogger",
  "version": "1.0.0",
  "description": "Work week time tracker",
  "main": "build/start.js",
  "scripts": {
    "build": "backend && npm install && tsc && cd ../frontend && npm install && npm run build"
  },
  "engines": {
    "node": "15.3.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
