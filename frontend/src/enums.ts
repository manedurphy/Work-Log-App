export enum Tasks {
  updateTask = 'UPDATE_TASK',
  updateTasks = 'UPDATE_TASKS',
}

export enum Users {
  setUser = 'SET_USER',
  clearUser = 'CLEAR_USER',
}

export enum Logs {
  setLog = 'SET_LOG',
}
