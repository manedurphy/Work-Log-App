{
  "name": "worklogger",
  "version": "1.0.0",
  "description": "Work week time tracker",
  "main": "build/start.js",
  "scripts": {
    "build": "npm install && cd backend && npm install && tsc && cd ../frontend && npm install && npm run build && cd ../"
  },
  "engines": {
    "node": "15.3.0"
  },
  "dependencies": {
    "typescript": "^4.0.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
